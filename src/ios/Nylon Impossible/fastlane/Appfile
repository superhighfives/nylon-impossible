app_identifier("com.superhighfives.Nylon-Impossible")
apple_id(ENV.fetch("APPLE_ID") { raise "APPLE_ID environment variable is required for Fastlane" })
team_id(ENV.fetch("TEAM_ID") { raise "TEAM_ID environment variable is required for Fastlane" })
